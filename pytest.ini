[pytest]
testpaths = tests
norecursedirs = release mobile/android/build mobile/android/app/build node_modules
markers =
    slow: spins up real native WebView2 windows; excluded by default, run with `pytest -m slow`
addopts = -m "not slow"
